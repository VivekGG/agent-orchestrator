---
name: git-workflow
description: Manage Git operations — branching strategies (GitFlow, trunk-based), conflict resolution, interactive rebase, cherry-pick, bisect. Use when setting up or managing the development workspace and tooling.
---

# Git Workflow Skill

Manage Git operations — branching strategies (GitFlow, trunk-based), conflict resolution, interactive rebase, cherry-pick, bisect.
